echo "early events were removed because we did not do extenstive search for common or shared between loci"
mping.excision.non_ref.number

echo "rils with shared excision events"
python Shared_excision.py
